<!-- Modal -->
<div class="modal fade" id="exampleModalCenter" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header bg-primary text-white">
        <h5 class="modal-title" id="exampleModalLongTitle">Contract</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        En cliquant sur "accepter", tu t'engages auprès de toi même pour réussir ce défi!
      </div>
      <div class="modal-footer bg-primary">
         <button type="button" class="btn btn-info text-white" data-dismiss="modal">Refuser</button>
        <%= link_to "Accepter", challenge_dares_path(@challenge), class: "btn btn-info text-white", method: :post %>
      </div>
    </div>
  </div>
</div>

<h1 class="text-center"><%= @challenge.name %></h1>

<p class="text-justify"> <%= @challenge.content %> </p>

<!-- Button trigger modal -->
<div class="flex-grow-1 bg-info text-black text-decoration-none text-center ">
  <button type="button" class="btn" data-toggle="modal" data-target="#exampleModalCenter">
    Go!
  </button>
</div>

<div id="map"
     style="width: 100%; height: 600px;"
     data-markers="<%= @markers.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
</div>
